# Database Configuration
# DATABASE_URL is OPTIONAL - smart defaults are used if not specified:
# - Development: Defaults to file:./data/local.db (no Docker needed!)
# - Integration tests: Automatically uses :memory: (fast, isolated)
# - Docker: Set to http://db:8080 in docker-compose.yaml

# Option 1: File-based SQLite (DEFAULT for local development)
# - Automatic if DATABASE_URL is not set
# - Fast, no Docker required
# - Enables running dev server with just `npm run dev`
# - WARNING: Multiple processes (dev + tests) can cause file locking issues
# DATABASE_URL=file:./data/local.db

# Option 2: HTTP LibSQL server (for Docker-based development)
# - No file locking issues
# - Requires Docker: `make up`
# - Uses LibSQL server container
# DATABASE_URL=http://localhost:8080

# Option 3: Production with Turso (cloud-hosted LibSQL)
# - For production deployments
# - Requires Turso account and database
# TURSO_DATABASE_URL=libsql://your-database.turso.io
# TURSO_AUTH_TOKEN=your_turso_auth_token

# NextAuth Configuration
AUTH_SECRET=txPLQWs8toKE251TIWiGS6abI4dJafPA5Kd/DTxou6q5
#AUTH_MAILGUN_KEY=your_mailgun_api_key
#AUTH_MAILGUN_FROM=your_email@domain.com

# PostHog Configuration
#NEXT_PUBLIC_POSTHOG_KEY=your_posthog_project_api_key
#NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com
#POSTHOG_ENABLED=1

# Optional: For Cloudflare D1 fallback
# AUTH_TRUST_HOST=true

# AI Configuration
# OPENAI_API_KEY=your_openai_api_key_here

# Kroger API Configuration
# KROGER_CLIENT_ID=your_client_id
# KROGER_CLIENT_SECRET=your_client_secret
# KROGER_REDIRECT_URI=http://localhost:3000/api/kroger/callback
# KROGER_API_BASE_URL=https://api.kroger.com/v1

# Port Configuration
# Why: Enables concurrent development, simplifies debugging, prevents port conflicts
WEB_PORT=3000
DB_PORT=8080
